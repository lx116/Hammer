<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    ios:Page.UseSafeArea="true"
    x:Class="Hammer.Views.ListProductView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:local="clr-namespace:Hammer.Views;assembly=Hammer"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>
        <StackLayout>
            <Button
                Command="{Binding GoToDetailsCommand}"
                CommandParameter="{x:Type local:AddProductView}"
                Text="Go To Details" />
            <Label Text="HOLA MUNDO" />
            <ListView ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Detail="{Binding Price, StringFormat='Price: {0:N2}'}" Text="{Binding Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>